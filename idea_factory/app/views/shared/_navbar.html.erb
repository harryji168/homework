<nav class="navbar navbar-expand-lg navbar-light bg-light d-flex justify-content-between border-btm border-dark">
<div class="navbar-brand">
        <div>
        <%= link_to root_path do %>
            <%= image_tag("logo.webp", width:"180",class:"image ml-4")%>   
        <% end %>         
        </div>
        <div>
        <%= link_to("Idea Factory", root_path)%>
        </div>
</div>
   <div class="d-flex flex-column">
        <div class="mt-4 mb-4">
                <% if user_signed_in? %>
                    Hello <%= link_to "/dashboard" do %> 
                        <%= current_user.full_name %>
                        <% end %> 
                    |
                   <% if current_user.try(:is_admin?) %>
                       <%= link_to "/dashboard" do %> 
                         Dashboard 
                        <% end %> 
                        |
                   <% end %>      
                    <%= link_to "Sign Out", session_path, method: :delete,
                    data: {confirm: 'Are you sure you want to log out?'}
                    %>
                <% else %>
                    <%= link_to "Login", new_session_path %> |
                    <%= link_to "Sign Up", new_user_path %>
                <% end %>
        </div>
        <div class="align-self-end mr-2">
            <%= link_to("New Idea", new_idea_path, class:"btn btn-outline-primary new-idea")%>
        </div>
    </div>
</nav>